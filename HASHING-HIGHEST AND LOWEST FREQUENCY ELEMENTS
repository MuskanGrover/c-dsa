#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    map<int, int> mpp; 
    for (int i = 0; i < n; i++) {
        mpp[arr[i]]++;
    }
    int maxFreq = 0, minFreq = INT_MAX;
    int maxElem = 0, minElem = 0;

    for (auto it : mpp) {
        if (it.second > maxFreq) {
            maxFreq = it.second;
            maxElem = it.first;
        }
        if (it.second < minFreq) {
            minFreq = it.second;
            minElem = it.first;
        }
    }

  
    cout << "Element with highest frequency: " << maxElem << " (Frequency: " << maxFreq << ")\n";
    cout << "Element with lowest frequency: " << minElem << " (Frequency: " << minFreq << ")\n";

    int q;
    cin >> q;
    while (q--) {
        int number;
        cin >> number;
        cout << "Frequency of " << number << ": " << mpp[number] << endl;
    }

    return 0;
}
